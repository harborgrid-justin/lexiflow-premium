# =============================================================================
# LexiFlow Premium - Neon PostgreSQL Production Configuration
# =============================================================================
# Enterprise-grade configuration for Neon serverless PostgreSQL
# Connection: Pooled endpoint with channel binding for maximum security
# =============================================================================

# =============================================================================
# NEON DATABASE CONNECTION
# =============================================================================
# Pooled connection string with SSL and channel binding
DATABASE_URL=postgresql://neondb_owner:npg_u71zdejvgHOR@ep-morning-violet-ahjfqnv2-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Individual connection parameters (for TypeORM config fallback)
DB_HOST=ep-morning-violet-ahjfqnv2-pooler.c-3.us-east-1.aws.neon.tech
DB_PORT=5432
DB_USERNAME=neondb_owner
DB_PASSWORD=npg_u71zdejvgHOR
DB_DATABASE=neondb

# SSL Configuration for Neon
DB_SSL=true
DB_SSL_REJECT_UNAUTHORIZED=false
DB_FORCE_SSL=true

# Connection Pool Settings (optimized for Neon pooled connections)
DB_POOL_SIZE=10
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=15000
DB_STATEMENT_TIMEOUT=60000

# Disable synchronize for production safety
DB_SYNCHRONIZE=false
DB_MIGRATIONS_RUN=true
DB_FALLBACK_SQLITE=false

# =============================================================================
# REDIS CONFIGURATION (local container)
# =============================================================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
NODE_ENV=production
PORT=5000
LOG_LEVEL=info
LOG_FORMAT=json

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
JWT_SECRET=lexiflow_enterprise_jwt_secret_neon_production_2025_secure
JWT_REFRESH_SECRET=lexiflow_enterprise_refresh_secret_neon_production_2025_secure
JWT_EXPIRATION=24h
BCRYPT_ROUNDS=12

# =============================================================================
# ENTERPRISE AGENTS CONFIGURATION
# =============================================================================
ENTERPRISE_AGENTS_ENABLED=true
AGENT_HEARTBEAT_INTERVAL=5000
AGENT_HEALTH_CHECK_INTERVAL=10000
AGENT_MAX_AGENTS=12
AGENT_ENABLE_COORDINATOR=true
AGENT_ENABLE_SCRATCHPAD=true
AGENT_AUTO_START=true

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
CORS_ORIGIN=*
CORS_CREDENTIALS=true

# =============================================================================
# RATE LIMITING
# =============================================================================
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# =============================================================================
# DEMO MODE
# =============================================================================
DEMO_MODE=false
SEED_DEMO_DATA=true

# =============================================================================
# TELEMETRY (optional)
# =============================================================================
OTEL_ENABLED=false
