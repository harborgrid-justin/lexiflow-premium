// config/prefetchConfig.ts

// This is a placeholder for actual prefetch configurations.
// In a real application, this would contain logic to map module IDs to data fetching functions.

export const PREFETCH_MAP: Record<string, { key: any; fn: () => Promise<any> }> = {};
