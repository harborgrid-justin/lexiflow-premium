# Backend Auto-Discovery Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend App                             │
│                      (http://localhost:3000)                     │
└─────────────────────────────────────────────────────────────────┘
                                │
                    ┌───────────┴───────────┐
                    │                       │
        ┌───────────▼─────────┐ ┌──────────▼──────────┐
        │  Backend Discovery  │ │  Data Source Context │
        │      Service        │ │   (User Control)     │
        │  🔄 ALWAYS ACTIVE   │ │  ⚠️ USER TRIGGERED  │
        └───────────┬─────────┘ └──────────┬──────────┘
                    │                       │
        ┌───────────▼─────────────────────────────────┐
        │   🔴 REAL-TIME Monitoring (30s intervals)   │
        │   GET /health → 5s timeout                  │
        │   Updates status: Available/Unavailable     │
        │   Works REGARDLESS of active data source    │
        └───────────┬─────────────────────────────────┘
                    │
        ┌───────────▼─────────────────────────────────┐
        │   Publishes Status to Subscribers           │
        │   ├─ useBackendDiscovery() Hook             │
        │   ├─ DataSourceSelector Component           │
        │   └─ BackendStatusIndicator Component       │
        └─────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                      Data Flow Decision                          │
└─────────────────────────────────────────────────────────────────┘

User Request
     │
     ▼
┌─────────────────────┐
│  DataService Facade │
│  (All data access)  │
└──────────┬──────────┘
           │
           ├─ Check: isBackendApiEnabled()
           │
     ┌─────┴─────┐
     │           │
     ▼           ▼
┌─────────┐  ┌──────────────┐
│ IndexDB │  │ Backend API  │
│  (Local)│  │ (PostgreSQL) │
└─────────┘  └──────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    User Switching Flow                           │
└─────────────────────────────────────────────────────────────────┘

1. User navigates to:
   Admin Console → System → Settings

2. DataSourceSelector shows:
   ┌──────────────────────────────────┐
   │ Backend Status: Online ✅        │
   │ Latency: 45ms                    │
   │ Version: 1.0.0                   │
   │ Last checked: 10:30:45 AM        │
   └──────────────────────────────────┘

3. User selects data source:
   ○ Local (IndexedDB)      [Always available]
   ● Backend (PostgreSQL)   [Requires backend online]
   ○ Cloud Sync             [Future feature]

4. Confirmation dialog:
   "Switch to Backend (PostgreSQL)?
    This will reload the application."
   
5. On confirm:
   localStorage.setItem('VITE_USE_BACKEND_API', 'true')
   window.location.reload()

6. After reload:
   - DataService routes to API
   - Status indicator shows "Online"
   - All data from PostgreSQL


┌─────────────────────────────────────────────────────────────────┐
│                   Component Hierarchy                            │
└─────────────────────────────────────────────────────────────────┘

App.tsx
 ├─ useEffect(() => backendDiscovery.start())  [Auto-start discovery]
 │
 ├─ ThemeProvider
 │   └─ DataSourceProvider                      [Manages switching]
 │       └─ ToastProvider
 │           └─ SyncProvider
 │               └─ WindowProvider
 │                   └─ InnerApp
 │                       ├─ Sidebar
 │                       │   └─ SidebarFooter
 │                       │       └─ BackendStatusIndicator  [Shows status]
 │                       │
 │                       └─ AdminPanel
 │                           └─ SystemSettings
 │                               └─ DataSourceSelector  [User control]


┌─────────────────────────────────────────────────────────────────┐
│                   Backend Integration                            │
└─────────────────────────────────────────────────────────────────┘

Backend (NestJS)
 ├─ http://localhost:5000
 │
 ├─ Health Endpoint:
 │   GET /health
 │   Response: { status: "ok", version: "1.0.0" }
 │
 ├─ API Endpoints:
 │   /api/v1/cases
 │   /api/v1/documents
 │   /api/v1/docket
 │   ... (40+ endpoints)
 │
 └─ Database:
     PostgreSQL + TypeORM


┌─────────────────────────────────────────────────────────────────┐
│     Status Indicator States (REAL-TIME, ALWAYS UPDATING)       │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────┬──────────────┬────────────────────────────────────┐
│ Data Source         │ Backend      │ Indicator Display                  │
│ (Active)            │ (Real-Time)  │ (Updates every 30s)                │
├─────────────────────┼──────────────┼────────────────────────────────────┤
│ IndexedDB (Local)   │ Available ✅ │ 🟦 Local (Backend Ready) + pulse  │
│ IndexedDB (Local)   │ Unavailable  │ ⬜ Local Only                      │
│ PostgreSQL (Backend)│ Available ✅ │ 🟩 Online (45ms) + pulse           │
│ PostgreSQL (Backend)│ Degraded ⚠️  │ 🟨 Degraded                        │
│ PostgreSQL (Backend)│ Unavailable  │ 🟥 Offline (retrying...)           │
└─────────────────────┴──────────────┴────────────────────────────────────┘

Note: Backend monitoring is INDEPENDENT of data source selection.
      Status updates occur every 30s regardless of which source is active.


┌─────────────────────────────────────────────────────────────────┐
│                   State Management                               │
└─────────────────────────────────────────────────────────────────┘

localStorage
 └─ VITE_USE_BACKEND_API: 'true' | 'false'
     │
     ├─ Read on app init
     ├─ Updated on data source switch
     └─ Persists across sessions

BackendDiscoveryService (Singleton)
 ├─ status: BackendStatus
 │   ├─ available: boolean
 │   ├─ healthy: boolean
 │   ├─ lastChecked: Date
 │   ├─ latency?: number
 │   └─ version?: string
 │
 └─ listeners: Set<Callback>
     └─ Notified on status change

DataSourceContext (React Context)
 ├─ currentSource: DataSourceType
 ├─ isBackendApiEnabled: boolean
 └─ switchDataSource(source)
     ├─ Update localStorage
     ├─ Invalidate queries
     └─ Reload page
