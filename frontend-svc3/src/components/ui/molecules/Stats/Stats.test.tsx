import React from 'react';
import { render, screen } from '@testing-library/react';
import { Stats } from './Stats';

describe('Stats Molecule', () => {
  it('renders correctly', () => {
    // render(<Stats />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
