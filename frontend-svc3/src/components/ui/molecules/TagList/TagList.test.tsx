import React from 'react';
import { render, screen } from '@testing-library/react';
import { TagList } from './TagList';

describe('TagList Molecule', () => {
  it('renders correctly', () => {
    // render(<TagList />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
