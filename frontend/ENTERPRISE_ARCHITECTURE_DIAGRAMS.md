# Enterprise React Architecture - Visual Diagrams

## System Overview

```
================================================================================
                    ENTERPRISE REACT ARCHITECTURE
                React v18 + React Router v7 + Context + Suspense
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                              BROWSER REQUEST                                 │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          REACT ROUTER v7                                     │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  ROUTE DEFINITION (routes.ts)                                         │  │
│  │  route("cases", "routes/cases/index.tsx")                             │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                      LOADER - DATA AUTHORITY LAYER                           │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  routes/cases/loader.ts                                               │  │
│  │  • clientLoader() - Fetches data from API                             │  │
│  │  • action() - Handles form submissions                                │  │
│  │  • Parallel data fetching: Promise.all([cases, invoices])             │  │
│  │  • Server-side validation                                             │  │
│  │  • Intent-based routing (create/update/delete)                        │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    ROUTE COMPONENT - DELEGATION LAYER                        │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  routes/cases/index.tsx                                               │  │
│  │  export default function CasesRoute() {                               │  │
│  │    return <CaseListPageContent />;                                    │  │
│  │  }                                                                     │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                   PAGE - DATA ORCHESTRATION LAYER                            │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  routes/cases/CaseListPage.tsx                                        │  │
│  │  • useLoaderData() - Access loader data                               │  │
│  │  • Initialize Provider with data                                      │  │
│  │  • Suspense boundaries (Phase 2)                                      │  │
│  │  • Error boundaries                                                   │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                   PROVIDER - DOMAIN LOGIC LAYER                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  routes/cases/CaseListProvider.tsx                                    │  │
│  │  • Domain state management (activeTab, filters)                       │  │
│  │  • Derived state (metrics, filteredCases)                             │  │
│  │  • Business logic (computations, aggregations)                        │  │
│  │  • Stable callbacks (setActiveTab, setFilters)                        │  │
│  │  • React 18 features (useTransition, useMemo, useCallback)            │  │
│  │  • Route-scoped context (NOT global)                                  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     VIEW - PURE PRESENTATION LAYER                           │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  routes/cases/CaseListView.tsx                                        │  │
│  │  • Pure render function (no side effects)                             │  │
│  │  • Context consumption only (useCaseList hook)                        │  │
│  │  • Events flow up (callbacks)                                         │  │
│  │  • Accessibility attributes                                           │  │
│  │  • NO business logic, NO data fetching, NO navigation                 │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       UI COMPONENTS - ATOMS/MOLECULES                        │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │  • Button, Input, Card, Table (atoms)                                 │  │
│  │  • PageHeader, MetricCard, SearchBar (molecules)                      │  │
│  │  • Sidebar, Modal, DataTable (organisms)                              │  │
│  │  • Fully reusable, prop-driven, stateless                             │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
┌──────────────────────────────────────────────────────────────────────────┐
│                           DATA FLOWS DOWN                                 │
└──────────────────────────────────────────────────────────────────────────┘

   USER NAVIGATES TO /cases
          │
          │  HTTP Request
          ▼
   ┌─────────────────┐
   │  ROUTER LOADER  │  clientLoader() runs BEFORE navigation
   │   (Server)      │  • Fetches cases from API
   │                 │  • Fetches invoices from API
   └────────┬────────┘  • Parallel: Promise.all([...])
            │
            │  Returns { cases, invoices }
            ▼
   ┌─────────────────┐
   │ ROUTE COMPONENT │  CasesRoute()
   │  (Delegation)   │  • Minimal, just delegates
   └────────┬────────┘  • Exports loader & action
            │
            │  Delegates to Page
            ▼
   ┌─────────────────┐
   │  PAGE (Orch.)   │  CaseListPageContent()
   │                 │  • useLoaderData() - gets {cases, invoices}
   └────────┬────────┘  • Initializes Provider
            │
            │  Passes initialData
            ▼
   ┌─────────────────┐
   │   PROVIDER      │  CaseListProvider({ initialCases, initialInvoices })
   │  (Domain)       │  • State: activeTab, filters
   │                 │  • Derived: metrics, filteredCases
   └────────┬────────┘  • Actions: setActiveTab, setFilters
            │
            │  Context API
            ▼
   ┌─────────────────┐
   │   VIEW (Pure)   │  CaseListView()
   │                 │  • useCaseList() - consumes context
   │                 │  • Renders UI with data
   └────────┬────────┘  • NO business logic
            │
            │  Props
            ▼
   ┌─────────────────┐
   │  UI COMPONENTS  │  <Button>, <Card>, <Table>
   │   (Reusable)    │  • Fully prop-driven
   └─────────────────┘  • Stateless


┌──────────────────────────────────────────────────────────────────────────┐
│                           EVENTS FLOW UP                                  │
└──────────────────────────────────────────────────────────────────────────┘

   USER CLICKS "New Case"
          │
          │  onClick event
          ▼
   ┌─────────────────┐
   │  UI COMPONENT   │  <Button onClick={handleCreate}>
   └────────┬────────┘
            │
            │  Callback
            ▼
   ┌─────────────────┐
   │   VIEW (Pure)   │  handleCreate() calls provider action
   └────────┬────────┘
            │
            │  Action
            ▼
   ┌─────────────────┐
   │   PROVIDER      │  Updates state, may call revalidator.revalidate()
   │  (Domain)       │  or use submit() for form submission
   └────────┬────────┘
            │
            │  Form submission (via useSubmit or fetcher)
            ▼
   ┌─────────────────┐
   │ ROUTER ACTION   │  action() in loader.ts
   │  (Server)       │  • Validates form data
   │                 │  • Calls DataService.cases.add()
   └────────┬────────┘  • Returns success/error or redirects
            │
            │  Revalidate
            ▼
   ┌─────────────────┐
   │  ROUTER LOADER  │  clientLoader() re-runs
   │                 │  • Fetches fresh data
   └─────────────────┘  • UI updates automatically


┌──────────────────────────────────────────────────────────────────────────┐
│                      NAVIGATION FLOWS SIDEWAYS                            │
└──────────────────────────────────────────────────────────────────────────┘

   USER CLICKS CASE
          │
          │  onClick
          ▼
   ┌─────────────────┐
   │   VIEW (Pure)   │  navigate(`/cases/${id}`)
   └────────┬────────┘
            │
            │  navigate() or <Link to="/cases/123">
            ▼
   ┌─────────────────┐
   │     ROUTER      │  Handles navigation
   │                 │  • Runs new route loader
   │                 │  • Unmounts old route
   └─────────────────┘  • Mounts new route
```

---

## Context Layering Hierarchy

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         CONTEXT HIERARCHY                                    │
│                   (Outer → Inner, Top → Bottom)                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  LAYER 1: INFRASTRUCTURE (providers/)                                        │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  EnvProvider         - Environment config                              │  │
│  │  ThemeProvider       - Visual theme (light/dark)                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────┬────────────────────────────────────────┘
                                     │ Can depend on Infrastructure only
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  LAYER 2: APPLICATION (contexts/)                                            │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  AuthContext         - User session (global)                           │  │
│  │  PermissionsContext  - Access control (global)                         │  │
│  │  ToastContext        - Notifications (global)                          │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────┬────────────────────────────────────────┘
                                     │ Can depend on Infrastructure + Application
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  LAYER 3: DOMAIN (routes/[feature]/)                                         │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  CaseListProvider    - Case list state (ROUTE-SCOPED)                  │  │
│  │  BillingProvider     - Billing state (ROUTE-SCOPED)                    │  │
│  │  DiscoveryProvider   - Discovery state (ROUTE-SCOPED)                  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────┬────────────────────────────────────────┘
                                     │ Can depend on all above layers
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  LAYER 4: UI (component-local)                                               │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  useState            - Local component state                           │  │
│  │  useReducer          - Complex component state                         │  │
│  │  useRef              - DOM refs, mutable values                        │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘

RULES:
• A context MAY ONLY depend on contexts ABOVE it
• Domain contexts MUST be route-scoped (NOT global)
• Global providers MUST NOT have domain knowledge
• UI state MUST NOT leak into domain contexts
```

---

## React 18 Concurrent Features Usage

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     REACT 18 CONCURRENT FEATURES                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│  useTransition               │  Non-urgent updates
│  ────────────────────────    │  • Tab switching
│  const [isPending,           │  • Filter changes (large lists)
│    startTransition] =        │  • Navigation
│    useTransition();          │
│                              │  UI remains responsive during update
│  startTransition(() => {     │
│    setActiveTab(newTab);     │
│  });                         │
└──────────────────────────────┘

┌──────────────────────────────┐
│  useMemo                     │  Expensive computations
│  ────────────────────────    │  • Metrics calculation
│  const metrics = useMemo(()  │  • Data filtering
│    => {                      │  • Data sorting
│    return computeMetrics(    │  • Aggregations
│      cases, invoices         │
│    );                        │  Only recomputes when deps change
│  }, [cases, invoices]);      │
└──────────────────────────────┘

┌──────────────────────────────┐
│  useCallback                 │  Stable references
│  ────────────────────────    │  • Event handlers
│  const handleClick =         │  • Callbacks passed to children
│    useCallback((id) => {     │  • Actions in context
│    navigate(`/case/${id}`);  │
│  }, [navigate]);             │  Prevents child re-renders
│                              │
└──────────────────────────────┘

┌──────────────────────────────┐
│  useSyncExternalStore        │  External mutable sources
│  ────────────────────────    │  • localStorage
│  const value =               │  • sessionStorage
│    useSyncExternalStore(     │  • WebSockets
│    subscribe,                │  • Third-party stores
│    getSnapshot,              │
│    getServerSnapshot          │  Concurrent-safe synchronization
│  );                          │
└──────────────────────────────┘

┌──────────────────────────────┐
│  Suspense (Phase 2)          │  Progressive rendering
│  ────────────────────────    │  • Code splitting
│  <Suspense                   │  • Data fetching
│    fallback={<Skeleton />}   │  • Streaming SSR
│  >                           │
│    <Await resolve={data}>    │  Shows fallback until ready
│      {(resolved) => <UI />}  │
│    </Await>                  │
│  </Suspense>                 │
└──────────────────────────────┘
```

---

## File Organization: Before vs After

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              BEFORE (Hybrid)                                 │
└─────────────────────────────────────────────────────────────────────────────┘

frontend/src/
├── features/
│   └── cases/
│       ├── components/
│       │   └── list/
│       │       ├── CaseManagement.tsx        (250+ lines, mixed concerns)
│       │       ├── CaseManagerContent.tsx
│       │       └── ...
│       └── index.tsx
├── components/
│   ├── cases/                                (DUPLICATE!)
│   └── ...
├── contexts/
│   ├── case/
│   │   └── CaseContext.tsx                   (GLOBAL - wrong scope)
│   └── ...
├── services/
│   └── infrastructure/
│       └── queryClient.ts                    (530 lines custom implementation)
└── routes/
    └── cases/
        └── index.tsx                         (Route only, fetches via useQuery)

ISSUES:
❌ Duplicate component locations (features/ vs components/)
❌ Business logic in presentation components
❌ Domain contexts are global (should be route-scoped)
❌ Custom React Query implementation
❌ No clear architectural boundaries


┌─────────────────────────────────────────────────────────────────────────────┐
│                        AFTER (Enterprise Standard)                           │
└─────────────────────────────────────────────────────────────────────────────┘

frontend/src/
├── routes/                                    🆕 PRIMARY location
│   └── cases/
│       ├── index.tsx                         (Route definition)
│       ├── loader.ts                         (Data authority)
│       ├── CaseListPage.tsx                  (Data orchestration)
│       ├── CaseListProvider.tsx              (Domain logic - ROUTE-SCOPED)
│       ├── CaseListView.tsx                  (Pure presentation)
│       └── components/
│           ├── CaseListSkeleton.tsx
│           └── CaseListError.tsx
│
├── providers/                                 🆕 GLOBAL ONLY
│   ├── RootProviders.tsx
│   ├── EnvProvider.tsx
│   └── ThemeProvider.tsx
│
├── contexts/                                  📝 APP-LEVEL ONLY
│   ├── AuthContext.tsx
│   └── PermissionsContext.tsx
│
├── components/                                📝 PURE UI ONLY
│   ├── atoms/                                (Button, Input, Badge)
│   ├── molecules/                            (SearchBar, Card)
│   └── organisms/                            (Sidebar, Modal)
│
└── lib/                                       📝 UTILITIES
    ├── api/
    ├── validation/
    └── utils/

BENEFITS:
✅ Single source of truth (routes/)
✅ Clear architectural boundaries
✅ Domain contexts route-scoped
✅ React Router loaders (no custom React Query)
✅ Scalable and maintainable
```

---

## Migration Timeline

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          6-WEEK MIGRATION PLAN                               │
└─────────────────────────────────────────────────────────────────────────────┘

WEEK 1: Data Flow Consolidation
├── Deprecate custom React Query
├── Migrate 10 high-traffic routes to loaders
├── Remove useEffect fetching
└── ✅ COMPLETE (Pilot: Cases module)

WEEK 2: Authentication Refactor
├── Backend: Session middleware + HTTP-only cookies
├── Frontend: Refactor AuthContext
├── Convert clientLoader → loader (30+ routes)
└── Enable true SSR

WEEK 3: Suspense + Streaming SSR
├── Implement defer() in loaders
├── Add Suspense boundaries
├── Create skeleton components
└── Measure performance improvements

WEEK 4: Folder Restructure
├── Migrate billing → Enterprise pattern
├── Migrate discovery → Enterprise pattern
├── Migrate documents → Enterprise pattern
└── Consolidate components/ and features/

WEEK 5: Context Layering Governance
├── Create providers/ (global infrastructure)
├── Move domain contexts to routes/
├── Remove legacy contexts
└── Document dependency rules

WEEK 6: Final Migration + Testing
├── Migrate remaining features
├── Update all imports
├── E2E testing
└── Performance benchmarking

┌──────────────────────────────────────┐
│  ESTIMATED TIMELINE                  │
│  • Aggressive: 6 weeks               │
│  • Realistic: 8-10 weeks (+ testing) │
│  • Risk Mitigation: Feature flags    │
└──────────────────────────────────────┘
```

---

## Performance Impact

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         PERFORMANCE IMPROVEMENTS                             │
└─────────────────────────────────────────────────────────────────────────────┘

BEFORE (Custom React Query + useEffect)
═════════════════════════════════════════════
Navigation to /cases
│
├─ Component mounts
│  └─ useEffect runs
│     └─ Fetch cases (500ms)
│        └─ Fetch invoices (300ms)  ← WATERFALL!
│           └─ Component re-renders
│              └─ Compute metrics (50ms)
│
Total: ~850ms + render time


AFTER (React Router Loaders + Memoization)
═════════════════════════════════════════════
Navigation to /cases
│
├─ Loader runs BEFORE navigation
│  └─ Promise.all([cases, invoices])
│     ├─ Fetch cases (500ms)     ← PARALLEL
│     └─ Fetch invoices (300ms)  ← PARALLEL
│
├─ Component mounts with data
│  └─ Provider memoizes metrics (50ms, only once)
│     └─ View renders
│
Total: ~550ms (500ms + 50ms)

IMPROVEMENT: 35% faster ⚡


PHASE 2 (With Suspense + Streaming SSR)
═════════════════════════════════════════════
Server-side:
├─ Loader runs on server
│  └─ defer({ critical, deferred })
│     ├─ Critical data (cases) - await
│     └─ Deferred data (invoices) - stream
│
├─ Stream HTML to browser
│  └─ Browser renders critical path
│     └─ Shows skeleton for deferred
│        └─ Hydrates when deferred resolves
│
Total TTI: ~300ms (critical path only)

IMPROVEMENT: 65% faster ⚡⚡⚡
```

---

**Visual Diagrams Complete** • System Architecture Documented • Ready for Team 🚀
