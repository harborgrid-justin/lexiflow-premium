import { render, screen } from '@testing-library/react';
import { CopyButton } from './CopyButton';

describe('CopyButton Atom', () => {
  it('renders correctly', () => {
    render(<CopyButton />);
    // Add specific assertions here
  });
});
