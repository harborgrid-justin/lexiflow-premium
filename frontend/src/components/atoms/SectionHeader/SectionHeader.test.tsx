import { render, screen } from '@testing-library/react';
import { SectionHeader } from './SectionHeader';

describe('SectionHeader Atom', () => {
  it('renders correctly', () => {
    render(<SectionHeader />);
    // Add specific assertions here
  });
});
