import React from 'react';
import { render, screen } from '@testing-library/react';
import { ActionRow } from './ActionRow';

describe('ActionRow Molecule', () => {
  it('renders correctly', () => {
    // render(<ActionRow />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
