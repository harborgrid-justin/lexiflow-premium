import React from 'react';
import { render, screen } from '@testing-library/react';
import { InfoGrid } from './InfoGrid';

describe('InfoGrid Molecule', () => {
  it('renders correctly', () => {
    // render(<InfoGrid />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
