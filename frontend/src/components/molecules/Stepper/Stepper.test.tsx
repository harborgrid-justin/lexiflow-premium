import React from 'react';
import { render, screen } from '@testing-library/react';
import { Stepper } from './Stepper';

describe('Stepper Molecule', () => {
  it('renders correctly', () => {
    // render(<Stepper />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
