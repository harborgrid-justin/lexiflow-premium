/**
 * Molecules Layer - Re-export from ui/molecules
 * 
 * This is a convenience re-export layer. Actual components live in /components/ui/molecules/
 * 
 * Usage:
 * import { Card } from '@/components/molecules';  // ← This works
 * import { Card } from '@/components/ui/molecules';  // ← This also works
 */

export * from '../ui/molecules';
