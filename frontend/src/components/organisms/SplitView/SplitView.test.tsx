import { render, screen } from '@testing-library/react';
import { SplitView } from './SplitView';

describe('SplitView Organism', () => {
  it('renders correctly', () => {
    // render(<SplitView />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
