import { render, screen } from '@testing-library/react';
import { TabbedView } from './TabbedView';

describe('TabbedView Organism', () => {
  it('renders correctly', () => {
    // render(<TabbedView />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
