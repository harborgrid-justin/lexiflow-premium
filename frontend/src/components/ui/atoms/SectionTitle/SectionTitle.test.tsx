import React from 'react';
import { render, screen } from '@testing-library/react';
import { SectionTitle } from './SectionTitle';

describe('SectionTitle Atom', () => {
  it('renders correctly', () => {
    render(<SectionTitle />);
    // Add specific assertions here
  });
});
