import React from 'react';
import { render, screen } from '@testing-library/react';
import { TabStrip } from './TabStrip';

describe('TabStrip Molecule', () => {
  it('renders correctly', () => {
    // render(<TabStrip />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
