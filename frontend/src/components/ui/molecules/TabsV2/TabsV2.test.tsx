import React from 'react';
import { render, screen } from '@testing-library/react';
import { TabsV2 } from './TabsV2';

describe('TabsV2 Molecule', () => {
  it('renders correctly', () => {
    // render(<TabsV2 />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
