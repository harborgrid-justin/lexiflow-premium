/**
 * Query Client Type Definitions
 *
 * Shared types for query client system.
 * Separated to maintain Fast Refresh compatibility.
 *
 * @module lib/query/types
 */

// Note: The custom QueryClient implementation uses a singleton pattern
// exported from @/services/infrastructure/queryClient
// so no additional types are needed here

export interface QueryClientConfig {
  // Placeholder for future configuration options
}
