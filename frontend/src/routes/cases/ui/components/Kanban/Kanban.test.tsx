import { render, screen } from '@testing-library/react';
import { Kanban } from './Kanban';

describe('Kanban Organism', () => {
  it('renders correctly', () => {
    // render(<Kanban />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
