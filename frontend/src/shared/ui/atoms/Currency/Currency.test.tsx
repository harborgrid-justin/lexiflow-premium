import React from 'react';
import { render, screen } from '@testing-library/react';
import { Currency } from './Currency';

describe('Currency Atom', () => {
  it('renders correctly', () => {
    render(<Currency />);
    // Add specific assertions here
  });
});
