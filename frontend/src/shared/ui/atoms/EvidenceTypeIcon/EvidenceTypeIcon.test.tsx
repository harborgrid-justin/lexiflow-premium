import { render, screen } from '@testing-library/react';
import { EvidenceTypeIcon } from './EvidenceTypeIcon';

describe('EvidenceTypeIcon Atom', () => {
  it('renders correctly', () => {
    render(<EvidenceTypeIcon />);
    // Add specific assertions here
  });
});
