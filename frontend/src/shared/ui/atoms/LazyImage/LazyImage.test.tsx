import { render, screen } from '@testing-library/react';
import { LazyImage } from './LazyImage';

describe('LazyImage Atom', () => {
  it('renders correctly', () => {
    render(<LazyImage />);
    // Add specific assertions here
  });
});
