import { render, screen } from '@testing-library/react';
import { LoadingSpinner } from './LoadingSpinner';

describe('LoadingSpinner Atom', () => {
  it('renders correctly', () => {
    render(<LoadingSpinner />);
    // Add specific assertions here
  });
});
