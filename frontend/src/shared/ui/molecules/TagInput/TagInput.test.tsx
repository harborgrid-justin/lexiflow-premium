import React from 'react';
import { render, screen } from '@testing-library/react';
import { TagInput } from './TagInput';

describe('TagInput Molecule', () => {
  it('renders correctly', () => {
    // render(<TagInput />);
    // expect(screen.getByText(/content/i)).toBeInTheDocument();
  });
});
