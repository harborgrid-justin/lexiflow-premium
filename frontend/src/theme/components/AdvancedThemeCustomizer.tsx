import { Grid, Layers, Layout, Palette, Sliders, Sparkles, Type, Zap } from 'lucide-react';
import React, { useState } from 'react';
import { useThemeCustomizer } from '../hooks/useThemeCustomizer';
import { ThemeDensity } from '../tokens'; export const AdvancedThemeCustomizer: React.FC = () => { const [ { mode, density, customTokens, status, message }, { setTheme, setDensity, updateColor: updateValue, saveChanges } ] = useThemeCustomizer(); type TabType = 'appearance' | 'colors' | 'typography' | 'layout' | 'effects' | 'advanced'; const [activeTab, setActiveTab] = useState<TabType>('colors'); const [expandedSections, setExpandedSections] = useState<Set<string>>(new Set(['brand'])); const isSaving = status === 'saving'; const formatKey = (key: string) => key.replace(/([A-Z])/g, ' $1').replace(/^./, str => str.toUpperCase()); const toggleSection = (section: string) => { const next = new Set(expandedSections); if (next.has(section)) { next.delete(section); } else { next.add(section); } setExpandedSections(next); }; const renderSection = (id: string, title: string, children: React.ReactNode, icon?: React.ReactNode) => { const isExpanded = expandedSections.has(id); return ( <div className="mb-4 border border-[var(--color-borderLight)] rounded-lg overflow-hidden"> <button onClick={() => toggleSection(id)} className="w-full px-4 py-3 bg-[var(--color-surfaceRaised)] hover:bg-slate-100 dark:hover:bg-slate-750 flex items-center justify-between transition-colors" > <div className="flex items-center gap-2"> {icon} <h3 className="text-sm font-semibold text-[var(--color-text)]">{title}</h3> </div> <span className={`transform transition-transform ${isExpanded ? 'rotate-180' : ''}`}>▼</span> </button> {isExpanded && ( <div className="p-4 bg-[var(--color-surface)] grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3"> {children} </div> )} </div> ); }; const tabs = [ { id: 'appearance', label: 'Appearance', icon: Sparkles }, { id: 'colors', label: 'Colors', icon: Palette }, { id: 'typography', label: 'Typography', icon: Type }, { id: 'layout', label: 'Layout & Spacing', icon: Layout }, { id: 'effects', label: 'Effects & Shadows', icon: Layers }, { id: 'advanced', label: 'Advanced', icon: Sliders }, ]; return ( <div className="bg-[var(--color-surface)] rounded-xl shadow-xl overflow-hidden border border-[var(--color-borderLight)] flex flex-col h-[calc(100vh-100px)]"> {/* Header */} <div className="p-6 border-b border-[var(--color-borderLight)] flex justify-between items-center bg-gradient-to-r from-slate-50 to-blue-50 dark:from-slate-800 dark:to-blue-900/20"> <div> <h2 className="text-2xl font-bold text-[var(--color-text)] dark:text-white flex items-center gap-2"> <Zap className="h-6 w-6 text-[var(--color-primary)]" /> Theme Studio Pro <span className="text-xs font-normal px-2 py-0.5 rounded-full bg-blue-100 text-blue-700 dark:bg-blue-900/30">150+ Options</span> </h2> <p className="text-sm text-[var(--color-textMuted)] mt-1">Complete design system customization with real-time preview</p> </div> <div className="flex items-center gap-4"> {message && ( <span className={`text-sm px-3 py-1 rounded-full animate-in fade-in ${status === 'success' ? 'bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-300' : 'bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-300'}`}> {message} </span> )} <button onClick={saveChanges} disabled={isSaving} className="px-6 py-2.5 bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white rounded-lg font-semibold shadow-md transition-all hover:shadow-lg disabled:opacity-50 flex items-center gap-2 active:scale-95" > {isSaving ? '💾 Saving...' : '🚀 Publish Theme'} </button> </div> </div> {/* Tabs */} <div className="flex border-b border-[var(--color-borderLight)] overflow-x-auto bg-[var(--color-surfaceRaised)]/50 /50"> {tabs.map(({ id, label, icon: Icon }) => ( <button key={id} onClick={() => setActiveTab(id as TabType)} className={`px-6 py-4 text-sm font-medium transition-all whitespace-nowrap border-b-2 flex items-center gap-2 ${activeTab === id ? 'border-blue-600 text-[var(--color-primary)] bg-[var(--color-surface)] ' : 'border-transparent text-[var(--color-textMuted)] hover:text-[var(--color-text)] dark:hover:text-slate-200 hover:bg-[var(--color-surface)]/50 dark:hover:bg-slate-800' }`} > <Icon className="h-4 w-4" /> {label} </button> ))} </div> {/* Content */} <div className="flex-1 overflow-y-auto p-6 bg-gradient-to-br from-slate-50 to-slate-100 dark:from-slate-900 dark:to-slate-800"> <div className="max-w-7xl mx-auto"> {/* Appearance Tab */} {activeTab === 'appearance' && ( <div className="space-y-6"> {renderSection("global","Global Settings", ( <> <div className="col-span-full space-y-2"> <label className="text-sm font-medium text-[var(--color-text)]">Color Mode</label> <div className="grid grid-cols-2 gap-2 p-1 bg-slate-100 rounded-lg"> <button onClick={() => setTheme('light')} className={`py-3 rounded-md text-sm font-medium transition-all ${mode === 'light' ? 'bg-[var(--color-surface)] text-[var(--color-text)] shadow-md scale-105' : 'text-[var(--color-textMuted)] hover:text-[var(--color-text)]'}`} > ☀️ Light Mode </button> <button onClick={() => setTheme('dark')} className={`py-3 rounded-md text-sm font-medium transition-all ${mode === 'dark' ? 'bg-slate-700 text-white shadow-md scale-105' : 'text-[var(--color-textMuted)] hover:text-[var(--color-text)] dark:hover:text-white'}`} > 🌙 Dark Mode </button> </div> </div> <div className="col-span-full space-y-2"> <label className="text-sm font-medium text-[var(--color-text)]">Information Density</label> <select value={density} onChange={(e) => setDensity(e.target.value as ThemeDensity)} className="w-full px-4 py-3 bg-[var(--color-surface)] border border-[var(--color-border)] rounded-lg text-sm focus:ring-2 focus:ring-blue-500 outline-none transition-shadow" > <option value="compact">📊 Compact (Data Heavy)</option> <option value="normal">📄 Normal (Standard)</option> <option value="comfortable">📖 Comfortable (Reader)</option> </select> <p className="text-xs text-[var(--color-textMuted)]">Adjusts spacing, sizing, and information density across the entire application.</p> </div> <TextInput label="Font Mode" value={customTokens.fontMode} onChange={(v) => updateValue(['fontMode'], v)} helpText="sans or serif" /> </> ), <Grid className="h-4 w-4" />)} </div> )} {/* Colors Tab */} {activeTab === 'colors' && ( <div className="space-y-4"> {renderSection("brand","Brand Colors (5)", ( <> {['primary', 'primaryDark', 'primaryLight', 'secondary', 'accent'].map(key => ( <ColorInput key={key} label={formatKey(key)} value={customTokens.colors[key as keyof typeof customTokens.colors] as string} onChange={(v) => updateValue(['colors', key], v)} /> ))} </> ), <Palette className="h-4 w-4" />)} {renderSection("backgrounds","Backgrounds (10)", ( <> {['background', 'backgroundSecondary', 'backgroundTertiary', 'surface', 'surfaceRaised', 'surfaceHighlight', 'surfaceOverlay', 'surfaceInput', 'surfaceActive', 'surfaceHover'].map(key => ( <ColorInput key={key} label={formatKey(key)} value={customTokens.colors[key as keyof typeof customTokens.colors] as string} onChange={(v) => updateValue(['colors', key], v)} /> ))} </> ), <Layers className="h-4 w-4" />)} {renderSection("borders","Borders (8)", ( <> {['border', 'borderLight', 'borderDark', 'borderFocus', 'borderError', 'borderSuccess', 'borderWarning', 'borderInfo'].map(key => ( <ColorInput key={key} label={formatKey(key)} value={customTokens.colors[key as keyof typeof customTokens.colors] as string} onChange={(v) => updateValue(['colors', key], v)} /> ))} </> ), <Grid className="h-4 w-4" />)} {renderSection("text","Text Colors (12)", ( <> {['text', 'textMuted', 'textInverse', 'textLink', 'textDisabled', 'textPlaceholder', 'textCode', 'textSuccess', 'textWarning', 'textError', 'textInfo', 'textAccent'].map(key => ( <ColorInput key={key} label={formatKey(key)} value={customTokens.colors[key as keyof typeof customTokens.colors] as string} onChange={(v) => updateValue(['colors', key], v)} /> ))} </> ), <Type className="h-4 w-4" />)} {renderSection("semantic","Semantic Colors (4)", ( <> {['success', 'warning', 'error', 'info'].map(key => ( <ColorInput key={key} label={formatKey(key)} value={customTokens.colors[key as keyof typeof customTokens.colors] as string} onChange={(v) => updateValue(['colors', key], v)} /> ))} </> ), <Sparkles className="h-4 w-4" />)} {renderSection("palette","Extended Palette (18)", ( <> {['slate50', 'slate100', 'slate200', 'slate300', 'slate400', 'slate500', 'slate600', 'slate700', 'slate800', 'slate900', 'blue400', 'blue500', 'blue600', 'emerald400', 'emerald500', 'amber400', 'rose400', 'indigo400'].map(key => ( <ColorInput key={key} label={formatKey(key)} value={customTokens.colors[key as keyof typeof customTokens.colors] as string} onChange={(v) => updateValue(['colors', key], v)} /> ))} </> ), <Palette className="h-4 w-4" />)} {renderSection("interactive","Interactive States (6)", ( <> {['hoverPrimary', 'hoverSecondary', 'activePrimary', 'activeSecondary', 'focusRing', 'disabled'].map(key => ( <ColorInput key={key} label={formatKey(key)} value={customTokens.colors[key as keyof typeof customTokens.colors] as string} onChange={(v) => updateValue(['colors', key], v)} /> ))} </> ), <Sliders className="h-4 w-4" />)} {renderSection("charts","Chart Colors (10)", ( <> {Object.entries(customTokens.colors.charts).map(([key, val]) => ( <ColorInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['colors', 'charts', key], v)} /> ))} </> ), <Grid className="h-4 w-4" />)} {renderSection("annotations","Annotations (8)", ( <> {Object.entries(customTokens.colors.annotations).map(([key, val]) => ( <ColorInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['colors', 'annotations', key], v)} /> ))} </> ), <Sparkles className="h-4 w-4" />)} {renderSection("gradients","Gradients (6)", ( <> {Object.entries(customTokens.colors.gradients).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['colors', 'gradients', key], v)} /> ))} </> ), <Layers className="h-4 w-4" />)} </div> )} {/* Typography Tab */} {activeTab === 'typography' && ( <div className="space-y-4"> {renderSection("fonts","Font Families (3)", ( <> {['fontSans', 'fontMono', 'fontSerif'].map(key => ( <TextInput key={key} label={formatKey(key)} value={customTokens.typography[key as keyof typeof customTokens.typography] as string} onChange={(v) => updateValue(['typography', key], v)} /> ))} </> ), <Type className="h-4 w-4" />)} {renderSection("weights","Font Weights (8)", ( <> {Object.entries(customTokens.typography.weights).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['typography', 'weights', key], v)} /> ))} </> ), <Sliders className="h-4 w-4" />)} {renderSection("sizes","Font Sizes (12)", ( <> {Object.entries(customTokens.typography.sizes).map(([key, val]) => ( <TextInput key={key} label={key.toUpperCase()} value={val as string} onChange={(v) => updateValue(['typography', 'sizes', key], v)} /> ))} </> ), <Type className="h-4 w-4" />)} {renderSection("lineHeight","Line Heights (5)", ( <> {Object.entries(customTokens.typography.lineHeight).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['typography', 'lineHeight', key], v)} /> ))} </> ), <Layout className="h-4 w-4" />)} {renderSection("letterSpacing","Letter Spacing (5)", ( <> {Object.entries(customTokens.typography.letterSpacing).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['typography', 'letterSpacing', key], v)} /> ))} </> ), <Sliders className="h-4 w-4" />)} </div> )} {/* Layout & Spacing Tab */} {activeTab === 'layout' && ( <div className="space-y-4"> {renderSection("spacing", `Spacing - ${density} (16)`, ( <> {Object.entries(customTokens.spacing[density]).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['spacing', density, key], v)} /> ))} </> ), <Layout className="h-4 w-4" />)} {renderSection("radius","Border Radius (8)", ( <> {Object.entries(customTokens.borderRadius).map(([key, val]) => ( <TextInput key={key} label={key.toUpperCase()} value={val as string} onChange={(v) => updateValue(['borderRadius', key], v)} /> ))} </> ), <Grid className="h-4 w-4" />)} {renderSection("layout","Layout Dimensions (15)", ( <> {Object.entries(customTokens.layout).map(([key, val]) => ( <TextInput key={key} type="number" label={formatKey(key)} value={String(val)} onChange={(v) => updateValue(['layout', key], v)} helpText="px" /> ))} </> ), <Layout className="h-4 w-4" />)} </div> )} {/* Effects & Shadows Tab */} {activeTab === 'effects' && ( <div className="space-y-4"> {renderSection("shadows","Shadows (10)", ( <> {Object.entries(customTokens.shadows).map(([key, val]) => ( <TextInput key={key} label={key.toUpperCase()} value={val as string} onChange={(v) => updateValue(['shadows', key], v)} /> ))} </> ), <Layers className="h-4 w-4" />)} {renderSection("blur","Blur Effects (4)", ( <> {Object.entries(customTokens.effects.blur).map(([key, val]) => ( <TextInput key={key} label={key.toUpperCase()} value={val as string} onChange={(v) => updateValue(['effects', 'blur', key], v)} /> ))} </> ), <Sparkles className="h-4 w-4" />)} {renderSection("opacity","Opacity Levels (4)", ( <> {Object.entries(customTokens.effects.opacity).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['effects', 'opacity', key], v)} /> ))} </> ), <Sliders className="h-4 w-4" />)} {renderSection("backdrop","Backdrop Filters (4)", ( <> {Object.entries(customTokens.effects.backdrop).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['effects', 'backdrop', key], v)} /> ))} </> ), <Layers className="h-4 w-4" />)} {renderSection("semantic-effects","Semantic Effects (7)", ( <> {Object.entries(customTokens.semantic).map(([key, val]) => ( <ColorInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['semantic', key], v)} /> ))} </> ), <Sparkles className="h-4 w-4" />)} </div> )} {/* Advanced Tab */} {activeTab === 'advanced' && ( <div className="space-y-4"> {renderSection("transitions","Transitions (8)", ( <> {Object.entries(customTokens.transitions).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['transitions', key], v)} /> ))} </> ), <Zap className="h-4 w-4" />)} {renderSection("zindex","Z-Index Layers (10)", ( <> {Object.entries(customTokens.zIndex).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['zIndex', key], v)} /> ))} </> ), <Layers className="h-4 w-4" />)} {renderSection("animation-duration","Animation Durations (4)", ( <> {Object.entries(customTokens.animations.duration).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['animations', 'duration', key], v)} /> ))} </> ), <Zap className="h-4 w-4" />)} {renderSection("animation-easing","Animation Easing (6)", ( <> {Object.entries(customTokens.animations.easing).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['animations', 'easing', key], v)} /> ))} </> ), <Sliders className="h-4 w-4" />)} {renderSection("animation-keyframes","Animation Keyframes (6)", ( <> {Object.entries(customTokens.animations.keyframes).map(([key, val]) => ( <TextInput key={key} label={formatKey(key)} value={val as string} onChange={(v) => updateValue(['animations', 'keyframes', key], v)} /> ))} </> ), <Sparkles className="h-4 w-4" />)} </div> )} </div> </div> {/* Footer Stats */} <div className="p-4 border-t border-[var(--color-borderLight)] bg-[var(--color-surfaceRaised)] flex items-center justify-between text-xs text-[var(--color-textMuted)]"> <div className="flex gap-6"> <span>150+ Theme Options</span> <span>•</span> <span>Real-time Preview</span> <span>•</span> <span>Auto-save to Backend</span> </div> <div className="text-[var(--color-primary)] font-medium"> LexiFlow Enterprise Design System </div> </div> </div> );
}; const ColorInput = ({ label, value, onChange }: { label: string, value: string, onChange: (val: string) => void }) => ( <div className="flex flex-col gap-1.5"> <label className="text-xs font-medium text-[var(--color-textMuted)]">{label}</label> <div className="flex items-center gap-2"> <div className="relative overflow-hidden w-10 h-10 rounded-lg shadow-sm ring-1 ring-slate-300 dark:ring-slate-600 cursor-pointer hover:ring-2 hover:ring-blue-500 transition-all flex-shrink-0"> <div className="absolute inset-0" style={{ backgroundColor: value }} /> <input type="color" value={value} onChange={(e) => onChange(e.target.value)} className="absolute inset-0 opacity-0 cursor-pointer w-full h-full" /> </div> <input type="text" value={value} onChange={(e) => onChange(e.target.value)} className="flex-1 px-3 py-2 text-xs bg-[var(--color-surface)] border border-[var(--color-border)] rounded-lg text-[var(--color-text)] outline-none focus:ring-2 focus:ring-blue-500 font-mono" placeholder="#000000" /> </div> </div>
); const TextInput = ({ label, value, onChange, type = 'text', helpText }: { label: string, value: string, onChange: (val: string) => void, type?: string, helpText?: string }) => ( <div className="flex flex-col gap-1.5"> <label className="text-xs font-medium text-[var(--color-textMuted)]">{label}</label> <input type={type} value={value} onChange={(e) => onChange(e.target.value)} className="w-full px-3 py-2 bg-[var(--color-surface)] border border-[var(--color-border)] rounded-lg text-sm text-[var(--color-text)] focus:border-blue-500 focus:ring-2 focus:ring-blue-500 outline-none transition-all" /> {helpText && <span className="text-xs text-slate-400">{helpText}</span>} </div>
); export default AdvancedThemeCustomizer;
