/**
 * Legacy @theme/tokens exports.
 *
 * The implementation lives under /src/lib/theme/tokens, so this file just forwards
 * the relevant types and helpers for existing import paths.
 */
export * from "@/lib/theme/tokens";
