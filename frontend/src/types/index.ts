/**
 * Shared Types
 * 
 * Domain types used across multiple features.
 * These are feature-level types, not global application types.
 */

// Export shared types here as they are created
// Example:
// export type { Document, DocumentMetadata } from './document';
// export type { Entity, EntityRelation } from './entity';
