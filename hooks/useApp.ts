/**
 * useApp Hook
 * Convenient access to app settings and state
 */

export { useAppContext as useApp } from '../context/AppContext';
export type { AppSettings, AppState } from '../context/AppContext';
