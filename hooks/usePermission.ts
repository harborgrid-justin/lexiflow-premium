/**
 * usePermission Hook
 * Convenient access to RBAC permission system
 */

export { usePermissionContext as usePermission } from '../context/PermissionContext';
export type { Permission, Role, PermissionRule, RolePermissions } from '../context/PermissionContext';
