
import { createContext } from "react";

/**
 * MatterContext: The single source of truth for all case-related data.
 * Pure JS file to ensure clear separation of business domain from UI.
 */
export const MatterContext = createContext(null);
